import java.net.URL;

public class MyJVMTest_2520 {

    static java.net.URL u = null;

    static String spec = "Q2raz5%I0~";

    static int start = 5;

    static int limit = 1543974446;

    String parseURL(java.net.URL u, String spec, int start, int limit) {
        throw new RuntimeException();
        return spec;
    }

    public static void main(String[] args) throws Exception {
        new MyJVMTest_2520().parseURL(u, spec, start, limit);
    }
}
